package com.gralliams.notekeeper

class DataManager {
    //Holds reference to courses in a HashMap
    var courses = HashMap<String, CourseInfo>()
    //holds reference to notes in An arrayList
    var notes = ArrayList<NoteInfo>()
}